{
  "name": "testrpcstorage",
  "version": "0.0.0",
  "scripts": {
    "bundle": "browserify src/app.js > public/app.js",
    "dev": "npm run bundle && npm run start",
    "start": "http-server -c-1 -p 12345 public"
  },
  "license": "MIT",
  "devDependencies": {
    "browserify": "^14.4.0",
    "http-server": "^0.10.0"
  },
  "dependencies": {
    "dat.gui": "^0.7.1",
    "ipfs": "^0.28.1",
    "ipfs-pubsub-room": "^1.2.0",
    "orbit-db": "^0.19.5",
    "stream-buffers": "^3.0.1"
  }
}
